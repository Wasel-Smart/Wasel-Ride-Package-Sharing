import { View, Text } from 'react-native';

export default function ExpoRouterStub() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Expo Router routes are disabled. Use React Navigation.</Text>
    </View>
  );
}
