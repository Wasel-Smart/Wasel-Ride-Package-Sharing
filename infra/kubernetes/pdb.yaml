apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-server
  namespace: wasel
  labels:
    app: api-server
    tier: backend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api-server
