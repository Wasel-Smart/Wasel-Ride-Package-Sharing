apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wasel-web
spec:
  tls:
    - hosts:
        - staging.wasel14.online
      secretName: wasel-web-staging-tls
  rules:
    - host: staging.wasel14.online
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wasel-web
                port:
                  name: http
