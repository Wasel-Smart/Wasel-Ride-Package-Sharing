apiVersion: v1
kind: Service
metadata:
  name: api-server
  namespace: wasel
  labels:
    app: api-server
    tier: backend
spec:
  selector:
    app: api-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
