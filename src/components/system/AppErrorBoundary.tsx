/**
 * Re-export shim — the canonical error-boundary implementation (including
 * AppErrorBoundary) lives in components/system/ErrorBoundary.tsx.
 */
export { AppErrorBoundary } from './ErrorBoundary';
